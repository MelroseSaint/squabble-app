/**
 * Squabble uses InstantDB.
 *
 * Setup is done in the Instant dashboard (schema + rules).
 *
 * 1) Create an Instant app: https://instantdb.com/dash
 * 2) Set VITE_INSTANTDB_APP_ID in your environment
 * 3) Run the app with `npm run dev`
 */

console.log('InstantDB setup happens in the dashboard. Set VITE_INSTANTDB_APP_ID to enable cloud sync.');
